export enum FeatureFlags {
  AdvancedSettings = "AdvancedSettings",
  AiBot = "AiBot",
  AiBotPro = "AiBotPro",
}

export enum FeatureFlagVisibility {
  ALL = "ALL",
  TEAM = "TEAM",
  USERS = "USERS",
}
