{
  "name": "@common/queues",
  "version": "1.9.10-rc.0",
  "author": "Alexander Ivanov @alexander2001i",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "ts-node-dev -r tsconfig-paths/register -r dotenv/config --transpile-only --no-notify ../queues/src/index.ts",
    "compile": "tsc -p tsconfig.json && tscpaths -p tsconfig.json -s ../queues/src -o ./dist",
    "start": "node -r dotenv/config dist/index.js"
  },
  "dependencies": {
    "@common/core": "^1.9.10-rc.0",
    "@prisma/client": "2.18.0",
    "bull": "^3.22.0"
  },
  "devDependencies": {
    "@types/bull": "^3.15.0"
  }
}
