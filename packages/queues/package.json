{
  "name": "@common/queues",
  "version": "1.9.10-rc.0",
  "author": "Alexander Ivanov @alexander2001i",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc-watch --onSuccess \"yarn fix:paths\"",
    "compile": "tsc -p tsconfig.json && tscpaths -p tsconfig.json -s ../queues/src -o ./dist",
    "fix:paths": "tscpaths -p tsconfig.json -s ./src -o ./dist"
  },
  "dependencies": {
    "@common/core": "^1.9.10-rc.0",
    "@prisma/client": "2.19.0",
    "bull": "^3.22.0"
  },
  "devDependencies": {
    "@types/bull": "^3.15.0"
  }
}
