model WireBankDetail {
  id String @id @default(uuid())

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  bankName    String

  line1 String?
  line2 String?

  city       String
  country    String
  district   String?
  postalCode String

  wires Wire[]
}