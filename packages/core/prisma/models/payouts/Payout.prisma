model Payout {
  id String @id @default(uuid())

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  status PayoutStatus @default(PendingApproval)

  amount      Int
  description String

  users User[]

  approvers PayoutApprover[]
  proposals FundingProposal[]

  wire Wire
}