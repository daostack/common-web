const BaseQueue = 'Common.Queue';

export const VotingQueue = `${BaseQueue}.Proposals.Voting`;
export const ProposalsQueue = `${BaseQueue}.Proposals`;

export const EventsQueue = `${BaseQueue}.Events`;
