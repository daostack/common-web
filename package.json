{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*",
    "packages/firebase/functions"
  ],
  "scripts": {
    "bootstrap": "yarn && lerna boostrap"
  }
}
